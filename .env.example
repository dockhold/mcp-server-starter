# A remote MCP server needs no required configuration to run.
#
# PORT is provided by Dockhold at runtime — don't set it yourself.
#
# Add any config your tools need (API keys, etc.) in the dashboard for plain
# values, or the Vault for secrets, and read them from process.env in server.js.
